# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

.env
.env.aws
.env.minio
.env.faynosync
docker-compose.full.yaml

*.pyc
*/*/node_modules

backend.tf
.terraform*

faynoSync
faynoSync_tests

.DS_Store

tmp*

tuf_keys/

docs/

# Local perf/index probes (build tag: indexbench)
server/handler/info/index_bench_test.go
server/handler/info/materialize_bench_test.go